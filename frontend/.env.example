# ClipCraft frontend configuration
#
# Base URL of the ClipCraft backend API (no trailing slash).
# The shared API client (src/lib/api.ts) prefixes every /api/v1/... request
# with this value. Defaults to http://localhost:5000 when unset.
VITE_API_BASE_URL=http://localhost:5000
